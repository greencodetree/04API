04-c 3. 네이버 얼굴 인식 - JSON 변환
프로그램을 실행시키면 많은 양의 정보가 배출되는데, pprint를 이용해서 정리할 수 없다
JSON 형태로 온 응답데이터를 파이썬의 딕셔너리로 변환해야 정리 가능
# JSON 은 문자열로 소통하는 웹통신에서 데이터 또는 객체 정보를 문자열로 표현하기 위해서 만들어진 포맷
# 딕셔너리와 비슷한 형태
import json, 그뒤 json.loads(response.text)로 변환 데이터를 data 변수에 저장
이제 data 변수를 딕셔너리로 변환시 pprint로 정리 가능
#form pprint import pprint 잊지 않기, 이미지 열기 전 파일및 폴더지정 올바르게 하기
딕셔너리로 구조화를 시킬 경우 facecount등의 특정 값을 접근할 수 있음

04-c 4. 네이버 얼굴 인식 - 응답 데이터 레퍼런스
출력되는 데이터가 뭔지 알기 위해선 네이버 개발자 - documents - clova ai api - cfr api 레퍼런스
응답 부문에서 JSON 데이터의 세부사항들과 각각 무엇을 의미하는지를 확인 가능
유명인 얼굴인식 api는 사진에서 유명인 얼굴을 인식해 해석, 에러 존재 가능

04-d 1. 파파고 - 가이드 따라하기
네이버개발자 - 파파고 에서 파이썬 구현예제 확인 가능
#papago NMT 번역 : 파파고 인공신경망 기계 기반 기술로 텍스트 번역
#papago SMT 번역 : 파파고 통계 기반 기계 번역 기술로 텍스트 번역
#encText 에 번역할 내용, data의 source는 내용의 원본 언어, target은 바꿀 언어(ko는 한국어, en은 영어)
이 코드 또한 JSON 형태의 출력정보를 다루기 위해 이전 코드처럼 import json, from pprint import pprint 등을 추가
#urlib.parse.quote 내용을 바꾸어 원하는 한국어를 영어로 번역 가능
파파고 레퍼런스에서 번역 언어 코드를 찾아 다른 언어 번역도 가능

04-d 2. 파파고 - 메모장 파일 번역
source.txt 라는 번역할 임의의 메모장 파일을 저장
영어 외 다른 언어로 번역시 문자부호방식이 아스키코드가 아닌 다른 방식일 수 있기 때문에 인코딩 방식을 UTF-8로 지정

